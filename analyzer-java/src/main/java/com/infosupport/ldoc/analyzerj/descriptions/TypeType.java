package com.infosupport.ldoc.analyzerj.descriptions;

/**
 * Enumeration for different top level types, such as class or enum.
 */
public enum TypeType {
  CLASS,
  INTERFACE,
  /**
   * Represents a Java record.
   */
  STRUCT,
  ENUM
}
